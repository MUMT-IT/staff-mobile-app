// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "io.jetsweep.vuetns"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
